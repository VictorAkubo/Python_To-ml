#(1)implicit: this is when python says hey am gonna convert the sum of two diffrent datatypes to a type like example converting a sum of int + float = float so its just the work of a basic calculator

#(2)explicit: is when tge programmer converts datatypes to different types using built in methods example(int(),str(),float(),list(),tuple(),set(),bool())
